# config.yaml

# Application settings
app:
  sslcert: "tests/cert/certificate.crt"
  sslkey: "tests/cert/private.key"
  testhttps: true

# RADIUS settings
radius:
  secret: "testing123"
  address: "<RADIUS_IP>:1812"
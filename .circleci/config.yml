version: 2.1

jobs:
  build:
    docker:
      - image: golang:latest

    steps:
      - checkout:
          path: /home/circleci/project/tdo
      #- run: go test

      - run:
          name: Print directory structure
          command: ls -R /home/circleci/project

      - run: 
          name: Compile the binary
          command: cd agent && go build -o tdo_agent .

workflows:
  version: 2
  build:
    jobs:
      - build
